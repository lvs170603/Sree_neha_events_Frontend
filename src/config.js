// Central API configuration
// Change this URL when switching between local development and production
const API_BASE_URL = 'https://sree-neha-events-backend.onrender.com';

export default API_BASE_URL;
